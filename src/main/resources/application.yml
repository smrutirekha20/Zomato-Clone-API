spring:
  application:
   GIT name=Zomato-Clone-API
  #DATASOURCE CONFIGURATION
  datasource:
    url: jdbc:mysql://localhost:3306/zomato_db?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
